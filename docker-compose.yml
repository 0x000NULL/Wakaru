version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: manabu-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: manabu_dev
      POSTGRES_PASSWORD: dev_password_123
      POSTGRES_DB: manabu_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U manabu_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
